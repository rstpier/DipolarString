# Scripts

Code backing the notes. Same environment as the released packages:
`pip install -r ../../v2.9/requirements.txt`.

Anything whose output is quoted in a note or in the manuscript ships with a check
under `../verification/`.
