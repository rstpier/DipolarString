# Notes

One file per attempt, named for the target, e.g. `proton-impedance-matching.md`.

Record failed attempts too, and why they failed. The manuscript does this — three
failed computations of `χ_vac` are recorded with their disagreement — and it is
what made the collective-suppression conclusion possible. A failure that is not
written down gets repeated.
